spring:
  boot:
    admin:
      client:
        username: jingjie
        password: xiuxiulovejingjie
        instance:
          metadata:
      context-path: /admin
  application:
    name: eureka-server

server:
  port: 8761

eureka:
  instance:
    health-check-url-path: /actuator/health
    metadata-map:
      startup: ${random.int}    #needed to trigger info and endpoint update after restart
      user:
        name: jingjie
        password: xiuxiulovejingjie
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_PEER_URL:http://localhost:8761/eureka/}

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS

